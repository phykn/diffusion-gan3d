domains:
  0:
    xy: [run/simul_height/slices/0]
    xz: [run/simul_height/slices/1]
    yz: [run/simul_height/slices/2]

num_phases: 3

# Side images retain their full 128-pixel thickness; training crops span 64.
crop_size: 64
lo_res_size: 32
hi_res_size: 64
