# Diffusion GAN3D

Independent 2D categorical slices are used to train a 3D denoising
diffusion-GAN. The implementation follows the chain-sampling idea in
Phan et al. (2024) and uses the posterior formulation of DDGAN.

```powershell
run_train.bat
```
